# Local development — override with your local PostgreSQL connection string.
# In production, DATABASE_URL is injected via ECS Secrets Manager (DB_CONNECTION).
DATABASE_URL="postgresql://trapac_admin:CHANGE_ME_LOCAL_DEV@localhost:5432/trapac_tas"
